/*아아디 비번찾기
router.get('/html/find-id.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/find-pw.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  
  router.get('/html/free_board.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/free-text.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/index.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/login.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });router.get('/html/market-board.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });router.get('/html/market-text.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/notice-board.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });router.get('/html/notice-text.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/pointshop.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/qna-board.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/qna-text.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/register.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/suggestions-board.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/suggestions-text.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });
  router.get('/html/write-post-page.html', function(req, res, next) {
    // res.render('index', { title: 'Express' });
  
    let is_logined = req.session.is_logined;
    let ss_email = req.session.ss_email;
    res.render('index', {
      title: 'Express',
      is_logined: is_logined,
      ss_email: ss_email
    })
  });*/